name: Security Scans

on:
  push:
    branches: [ main, master ]
  pull_request:

permissions:
  contents: read
  security-events: write

jobs:
  semgrep:
    uses: your-org/secure-ci-templates/.github/workflows/semgrep.yml@main
    with:
      scan_path: "."
